This Project creates a cloud storage platform that can switch between different cloud storage providers (such as GCP, AWS and AliBaba).

The backend is written in Java Spring Boot and the frontend is written in Vue.